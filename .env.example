# Server Configuration
SERVER_IP=localhost
API_PORT=8080

# Proxy Authentication (for proxy instances)
PROXY_USERNAME=admin
PROXY_PASSWORD=secure123

# Database
DATABASE_PATH=./data/proxies.db

# Optional - Auto-reset check interval (seconds)
AUTO_RESET_INTERVAL=10
